#!/usr/bin/env ruby
# encoding: utf-8

require 'rubygems'

begin
  require 'git-shizzle/cli'
rescue LoadError => e
  warn 'Could not load "git-shizzle/cli"'
  exit -1
end

begin
  GitShizzle::Cli.start
rescue GitShizzle::Error => error
  $stderr.puts error.message
  exit 1
end
